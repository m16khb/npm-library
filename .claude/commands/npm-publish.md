---
description: npm 패키지 배포 - 라이브러리명을 인자로 받아 테스트, 빌드, 배포를 수행합니다.
---

## 사용자 입력

```text
$ARGUMENTS
```

**인자**: 배포할 패키지 이름 (예: `async-utils`, `nestjs-async-utils`, `traceable`)

인자가 비어있으면 사용자에게 패키지 이름을 물어보세요.

---

## 배포 프로세스

### 1. 패키지 확인

- `packages/` 디렉토리에서 입력받은 패키지명과 일치하는 디렉토리 찾기
- 패키지 경로: `packages/{패키지명}/`
- 패키지가 존재하지 않으면 에러 메시지와 함께 사용 가능한 패키지 목록 출력

### 2. 패키지 정보 확인

- `packages/{패키지명}/package.json` 읽기
- 다음 정보 출력:
  - 패키지명 (name)
  - 현재 버전 (version)
  - 설명 (description)
  - 접근 설정 (publishConfig.access)

### 3. 코드 검증 (순차 실행)

패키지 디렉토리에서 다음 명령어를 **순차적으로** 실행:

```bash
# 패키지 디렉토리로 이동
cd packages/{패키지명}

# 3-1. 테스트 실행
pnpm test

# 3-2. 타입 체크 (테스트 통과 후)
pnpm typecheck

# 3-3. 빌드 (타입 체크 통과 후)
pnpm build
```

각 단계가 실패하면 **즉시 중단**하고 에러 내용 출력.

### 4. 패키지 검증

```bash
# 패키지 내용 미리보기
npm pack --dry-run
```

출력 결과를 사용자에게 보여주고 다음 항목 확인:
- [ ] `dist/` 폴더만 포함되어 있는지
- [ ] 민감 정보 (.env 등) 제외되었는지
- [ ] 패키지 크기가 적절한지

### 5. 버전 확인 및 결정

현재 버전과 npm에 배포된 최신 버전 비교:

```bash
# npm에 배포된 버전 확인 (없으면 첫 배포)
npm view {패키지_full_name} version 2>/dev/null || echo "첫 배포"
```

**사용자에게 질문**:
- 현재 버전: `x.y.z`
- npm 최신 버전: `a.b.c` (또는 "첫 배포")
- 버전 변경 옵션:
  1. `patch` - 버그 수정 (x.y.z → x.y.z+1)
  2. `minor` - 새 기능 추가 (x.y.z → x.y+1.0)
  3. `major` - Breaking Change (x.y.z → x+1.0.0)
  4. `현재 버전 유지` - 버전 변경 없이 배포
  5. `취소` - 배포 취소

버전 변경이 필요하면:

```bash
npm version {선택한_옵션} --no-git-tag-version
```

### 6. 최종 확인

배포 전 최종 확인 메시지:

```
📦 배포 정보
━━━━━━━━━━━━━━━━━━━━
패키지: {패키지_full_name}
버전: {최종_버전}
접근: public
━━━━━━━━━━━━━━━━━━━━

배포를 진행하시겠습니까? (yes/no)
```

### 7. 배포 실행

```bash
npm publish --access public
```

**2FA 활성화된 경우**: OTP 입력 안내

### 8. 배포 후 검증

```bash
# npm 정보 확인
npm view {패키지_full_name}

# 버전 확인
npm view {패키지_full_name} version
```

### 9. 완료 메시지

```
✅ 배포 완료!

📦 {패키지_full_name}@{버전}
🔗 https://www.npmjs.com/package/{패키지_full_name}

다음 단계:
1. npm 페이지에서 배포 확인
2. 새 프로젝트에서 설치 테스트: npm install {패키지_full_name}
3. Git 태그 푸시 (선택): git push origin v{버전}
```

---

## 에러 처리

| 에러 | 원인 | 해결 방법 |
|------|------|----------|
| `npm ERR! 403` | 로그인 필요 | `npm login` 실행 |
| `npm ERR! 402` | 스코프 패키지 private | `--access public` 확인 |
| `version already exists` | 버전 중복 | 버전 업데이트 필요 |
| `ENEEDAUTH` | 인증 실패 | `npm login` 실행 |

---

## 예시

```bash
# async-utils 패키지 배포
/npm-publish async-utils

# nestjs-async-utils 패키지 배포
/npm-publish nestjs-async-utils

# traceable 패키지 배포
/npm-publish traceable
```
